#include <iostream>
using namespace std;
int main(){
    string a = 'table'; 
    
    string b;
    for (int i = 0; i < a.length(); i++)
    b = b + '-';
    cout << b << endl;
    int so_lan = 10;
    string c;
    while (so_lan > 0 && b != a) {

    }
    return 0;
}